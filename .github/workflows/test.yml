# This workflow is superseded by ci.yml
# Kept as a no-op to avoid breaking existing references
name: VoxiraApp — Full Test Suite (deprecated)

on:
  workflow_dispatch:   # manual only — no longer runs on push

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Redirected
        run: echo "This workflow is deprecated. Use ci.yml instead."
